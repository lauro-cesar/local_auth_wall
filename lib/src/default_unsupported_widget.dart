import 'package:flutter/material.dart';

///
class DefaultUnsupportedWidget extends StatelessWidget {
  ///
  const DefaultUnsupportedWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
